<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="#">Community Support</a>
  </div>
</nav>

<div class="container mt-4">
  <div class="row">
    <!-- Left Section - Post Submission -->
    <div class="col-md-4">
      <div class="card p-4 shadow">
        <h5 class="mb-3">Share Your Story</h5>
        <textarea [(ngModel)]="newPost.content" class="form-control" placeholder="What's on your mind?" rows="4"></textarea>
        <button class="btn btn-primary mt-3 w-100" (click)="submitPost()">Post</button>
      </div>
    </div>

    <!-- Right Section - Posts List -->
    <div class="col-md-8">
      <div class="posts-container">
        <h4 class="mb-3">Community Posts</h4>
        <div class="card post-card shadow-sm p-3 mb-3" *ngFor="let post of posts">
          <div class="d-flex justify-content-between align-items-center">
            <h5 class="post-title">{{ post.title }}</h5>
            <span class="comment-icon" (click)="openCommentsModal(post)">
              💬 {{ post.comments.length }} 
            </span>
          </div>
          <p class="post-content">{{ post.content }}</p>
          <button class="btn btn-outline-primary btn-sm w-100" (click)="openCommentsModal(post)">
            View Comments
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Comments Modal -->
<div *ngIf="selectedPost" class="modal fade show" style="display: block; background: rgba(0, 0, 0, 0.5);" (click)="closeModal()">
  <div class="modal-dialog modal-dialog-centered" (click)="$event.stopPropagation()">
    <div class="modal-content p-4">
      <h4>Comments on "{{ selectedPost.title }}"</h4>
      <div class="comments-list mb-3">
        <p *ngFor="let comment of selectedPost.comments" class="border-bottom py-2">
          {{ comment.content }}
        </p>
      </div>
      <textarea [(ngModel)]="newComment" class="form-control" placeholder="Write a comment..."></textarea>
      <button class="btn btn-success mt-2 w-100" (click)="addComment()">Comment</button>
      <button class="btn btn-secondary mt-2 w-100" (click)="closeModal()">Close</button>
    </div>
  </div>
</div>
